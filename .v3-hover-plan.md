# v3 — L2 BOM Table Hover Comment 实现计划

> 当前状态：代码已写入 `BOMTable.tsx`，构建通过，CSS 类已生成。但 **用户在浏览器中没有看到悬停效果**，需要排查 + 修复。

## ⚠️ 首要排查：确认用户在看正确的页面

ISG Cost Guardian 有多 tab，默认是 "Program Dashboard"。**用户需要点击左侧 Sidebar 的 "L2 BOM Analysis" 才能看到 BOM 表格。** 确认一下。

如果确认在正确页面后仍然无效，往下排查：

## 修改的文件

`src/components/dashboard/BOMTable.tsx`

## 实现方案（已写入代码）

### 核心思路
使用 Tailwind `group` + `group-hover:` 纯 CSS 方案（不依赖 React state），避免 `<td>` 元素上 max-width transition 不生效的问题。

### 改动点

1. **行高亮（`<tr>`）**
```
className={`group border-l-[3px] border-l-transparent
  ${status === "Blocked" ? 'hover:border-l-destructive hover:bg-red-50/40'
    : status === "At Risk" ? 'hover:border-l-amber-500 hover:bg-amber-50/40'
    : 'hover:border-l-primary hover:bg-secondary/40'}
`}
```
- 左侧 3px border 从 transparent 切换为状态对应颜色
- 背景微变色

2. **Insight 列（最后一个 `<td>` + 内部 `<span>`）**
```
<td className="py-2 pl-2 pr-0 group-hover:pr-3">
  {hasInsight && (
    <span className="inline-block max-w-0 opacity-0 
      group-hover:max-w-[300px] group-hover:opacity-100
      overflow-hidden whitespace-nowrap transition-all duration-300 ease-out">
      {comp.aiInsight}
    </span>
  )}
</td>
```
- `<span>` 用 `inline-block` + `max-w` 做滑出动画
- `overflow-hidden` + `whitespace-nowrap` 保证文本不溢出
- 无 insight 时不渲染 span

## 如果悬停仍无效，排查方向

### A. Tailwind 配置
检查 `tailwind.config.ts`（或 `tailwind.config.js`），确认：
- `content` 路径包含 `./src/**/*.{ts,tsx}`
- 没有 `safelist` 限制
- 确认项目使用的是 Tailwind v3+（JIT 模式，自动扫描 class）

### B. CSS 优先级冲突
- 在浏览器 DevTools 检查 hover 时 `<tr>` 的 computed styles
- 确认 `border-left-color` 是否从 `transparent` 切换为目标色
- 确认 `max-width` 是否从 `0` 切换到 `300px`

### C. 也许 group-hover 在 `<tr>` 上不生效？
如果纯 CSS group hover 在 table 环境下有问题，**备选方案——退回 React state**：
```tsx
const [hoveredId, setHoveredId] = useState<number | null>(null);

// tr 上：
onMouseEnter={() => setHoveredId(id)}
onMouseLeave={() => setHoveredId(null)}
className={`... ${hoveredId === id ? 'border-l-destructive bg-red-50/40' : 'border-l-transparent'}`}

// span 上：用 style 直接控制 max-width
style={{
  maxWidth: hoveredId === id ? '300px' : '0px',
  opacity: hoveredId === id ? 1 : 0,
}}
```

### D. 最暴力方案——直接用 style
如果 Tailwind 类不生效，用内联 style 硬写：
```tsx
style={{
  maxWidth: hoveredId === id && hasInsight ? '300px' : '0px',
  opacity: hoveredId === id && hasInsight ? 1 : 0,
  transition: 'all 300ms ease-out',
  overflow: 'hidden',
  whiteSpace: 'nowrap',
  display: 'inline-block',
}}
```

## 验证方法

修改后：
1. `npm run dev` 启动
2. 打开 `http://localhost:5174/`
3. **点击左侧 Sidebar → "L2 BOM Analysis"**
4. 鼠标悬停任意表格行
5. 预期：行左侧出现彩色竖线 + 背景微变 + 行尾滑出 insight 文本

## 数据说明

Excel 上传已支持 `AI_Insight` 列（BOM sheet），格式：
```
Platform | Component | Category | DCE | Quote | Ch1 | Ch2 | Material | Status | AI_Insight
SR650 V4 | Motherboard | ECAD | 498 | 508 | 481 | 470 | FR4 | At Risk | Layer count reduction may save $15-20
```

## 知识库文档

设计模式已沉淀到：`workspace/design-knowledge/table-hover-comment-pattern.md`
